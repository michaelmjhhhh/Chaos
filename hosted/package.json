{
  "name": "chaos-hosted-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Serverless proxy that grants Chaos users a free naming trial through Agnes AI, holding the real key server-side and enforcing per-device + global limits.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3"
  },
  "devDependencies": {
    "@vercel/node": "^5.0.2",
    "typescript": "^5.6.3"
  }
}
